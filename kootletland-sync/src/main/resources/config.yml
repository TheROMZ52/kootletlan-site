database:
  url: "jdbc:mysql://HOST:3306/DATABASE?useSSL=true&requireSSL=true"
  username: "USERNAME"
  password: "PASSWORD"

website:
  url: "https://kootletlan-site.vercel.app"

sync:
  interval-seconds: 60
  sync-on-join: true
  sync-on-quit: true

notifications:
  poll-seconds: 15
