# Changelog

## 2.0 — طراحی مجدد و رفع باگ‌ها

### امنیت
- SQL injection در `/player/[username]` و اکانت: escape قدیمی بک‌اسلش را رد می‌کرد. حالا همه‌ی کوئری‌ها پارامتری‌اند و نام بازیکن اعتبارسنجی می‌شود.
- سابقه‌ی خرید دیگر بر اساس نام Minecraft «تأییدنشده» نمایش داده نمی‌شود (هر کسی می‌توانست نام دیگری را ثبت کند).
- UUID از پروفایل عمومی حذف شد. هدرهای امنیتی اضافه شد.

### باگ‌ها
- `proxy.ts` بدون متغیرهای Supabase کل سایت را 500 می‌کرد؛ حالا guard دارد و فقط روی مسیرهای لازم اجرا می‌شود.
- ذخیره‌ی نام Minecraft وقتی ردیف پروفایل وجود نداشت بی‌صدا شکست می‌خورد ولی «ذخیره شد» نشان می‌داد.
- پاراگراف‌بندی خبرها کار نمی‌کرد (split روی `\n` لفظی)؛ تاریخ خبرها نمایش داده نمی‌شد و تاریخ‌های پیش‌فرض هر بار عوض می‌شد.
- prefix رنک‌ها (`&#RRGGBB&l…`) خام نمایش داده می‌شد؛ حالا با همان رنگ داخل بازی رندر می‌شود.
- ذخیره‌ی نام Minecraft روی Supabase با «permission denied» شکست می‌خورد (`authenticated` هیچ UPDATE نداشت)؛ فقط روی ستون‌های امن مجاز شد، بدون دسترسی به `coins`.
- `metadataBase` به آدرس سرور بازی اشاره می‌کرد نه سایت؛ ناهماهنگی hydration در تاریخ‌ها؛ منوی موبایل بعد از کلیک باز می‌ماند.
- کارت «تأخیر» همیشه «—» بود (API آن را نمی‌دهد)؛ وقتی چکر قطع بود «آفلاین» نشان داده می‌شد، حالا «نامشخص».
- کپی آدرس بدون clipboard API (مرورگر قدیمی/HTTP) هیچ کاری نمی‌کرد.
- `docs/layerbase-schema.sql` سینتکس PostgreSQL داشت و روی MariaDB اجرا نمی‌شد.

### کارایی
- اخبار و فروشگاه دیگر برای هر بازدید رندر نمی‌شوند (ISR ۶۰ ثانیه)؛ API وضعیت کش می‌شود و یک poll مشترک دارد.
- فونت‌ها از npm و self-hosted؛ Vazirmatn قبلاً هرگز لود نمی‌شد. وابستگی بلااستفاده‌ی `@neondatabase/serverless` حذف شد.

### ظاهر و محتوا
- طراحی کامل از نو (فارسی، ریسپانسیو، بدون متن انگلیسی اضافه)، صفحه‌ی 404/خطا، `robots`، `sitemap`، آیکون.
- دیسکورد با روبیکا (`kootletland`) جایگزین شد.
- فروشگاه فقط از Layerbase (`store_products`) می‌خواند؛ دیگر پکیج و قیمت ساختگی نشان نمی‌دهد.
- صفحه‌ی رأی تا وقتی لینکی اضافه نشده از منو پنهان است.
